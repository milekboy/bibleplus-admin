export function formatDate(value: string | number | Date, options: Intl.DateTimeFormatOptions = { dateStyle: "medium" }) { const date = new Date(value); return Number.isNaN(date.valueOf()) ? "—" : new Intl.DateTimeFormat("en-NG", options).format(date); }
export function formatRelativeTime(value: string | number | Date) { const seconds = Math.round((new Date(value).valueOf() - Date.now()) / 1000); if (!Number.isFinite(seconds)) return "—"; const ranges: [Intl.RelativeTimeFormatUnit, number][] = [["year", 31_536_000], ["month", 2_592_000], ["week", 604_800], ["day", 86_400], ["hour", 3_600], ["minute", 60]]; const formatter = new Intl.RelativeTimeFormat("en", { numeric: "auto" }); for (const [unit, size] of ranges) if (Math.abs(seconds) >= size) return formatter.format(Math.round(seconds / size), unit); return formatter.format(seconds, "second"); }
export function formatNumber(value: number) { return new Intl.NumberFormat("en-NG").format(value); }
export function formatBoolean(value: boolean) { return value ? "Yes" : "No"; }
export function safeDownloadFilename(value: string, fallback = "bibleplus-export") { const clean = value.replace(/[<>:"/\\|?*\u0000-\u001F]/g, "-").replace(/\s+/g, " ").trim(); return clean || fallback; }
