import { NextResponse } from "next/server";
import { buildBackendUrl, fetchWithTimeout } from "@/lib/api/server";
import { ACCESS_TOKEN_COOKIE, ADMIN_PROFILE_COOKIE, isTokenExpired, sessionCookieOptions } from "@/lib/auth/token";
function cookie(request: Request, name: string) { return request.headers.get("cookie")?.split(";").map((part) => part.trim()).find((part) => part.startsWith(`${name}=`))?.slice(name.length + 1); }
function expire(response: NextResponse) { response.cookies.set(ACCESS_TOKEN_COOKIE, "", { ...sessionCookieOptions(), maxAge: 0 }); response.cookies.set(ADMIN_PROFILE_COOKIE, "", { ...sessionCookieOptions(), maxAge: 0 }); }
async function handler(request: Request, context: RouteContext<"/api/backend/[...path]">) { const encoded = cookie(request, ACCESS_TOKEN_COOKIE), token = encoded ? decodeURIComponent(encoded) : undefined; if (!token || isTokenExpired(token)) { const response = NextResponse.json({ message: "Your session has expired. Please sign in again." }, { status: 401 }); expire(response); return response; } const { path } = await context.params; if (!path.length || path.some((segment) => !segment || segment === "." || segment === "..")) return NextResponse.json({ message: "Invalid backend path." }, { status: 400 }); const source = new URL(request.url), headers = new Headers({ Authorization: `Bearer ${token}`, Accept: request.headers.get("accept") ?? "application/json" }); headers.set("Accept-Encoding", "identity"); const contentType = request.headers.get("content-type"), idempotency = request.headers.get("idempotency-key"); if (contentType) headers.set("Content-Type", contentType); if (idempotency) headers.set("Idempotency-Key", idempotency); const hasBody = !["GET", "HEAD"].includes(request.method); try { const backend = await fetchWithTimeout(buildBackendUrl(path.map(encodeURIComponent).join("/"), source.search), { method: request.method, headers, body: hasBody ? request.body : undefined, cache: "no-store", redirect: "manual", duplex: hasBody ? "half" : undefined } as RequestInit & { duplex?: "half" }); const outputHeaders = new Headers({ "Cache-Control": "no-store" }); for (const name of ["content-type", "content-disposition"]) { const value = backend.headers.get(name); if (value) outputHeaders.set(name, value); } const response = new NextResponse(backend.body, { status: backend.status, statusText: backend.statusText, headers: outputHeaders }); if (backend.status === 401) expire(response); return response; } catch (error) { const timeout = error instanceof Error && error.name === "AbortError"; return NextResponse.json({ message: timeout ? "The backend request timed out." : "The backend could not be reached." }, { status: timeout ? 504 : 502 }); } }
export const dynamic = "force-dynamic"; export const runtime = "nodejs";
export const GET = handler; export const POST = handler; export const PUT = handler; export const PATCH = handler; export const DELETE = handler; export const HEAD = handler; export const OPTIONS = handler;
